export enum Tense {
  Default,
  Past,
  Present,
  Future,
}

export enum Aspect {
  Default,
  Inchoative,
  Progressive,
  Completive,
}

export enum Mood {
  Realis,
  Irrealis,
}

export enum Case {
  Nominative,
  Accusative,
  Dative,
}

export enum Klass {

}